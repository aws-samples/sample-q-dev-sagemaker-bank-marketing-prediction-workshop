# SageMaker Python SDK v3 (3.0 released 2025-11-20; 3.12.0 patch released 2026-05-20).
# Pinned exactly so workshop participants run identical software across cohorts.
sagemaker==3.12.0
sagemaker-core==2.12.0    # Processing primitives (installs into sagemaker/core/)

# AWS SDK
boto3==1.43.14
botocore==1.43.14

# ML libraries used by training/inference scripts
xgboost==3.2.0
pandas==2.2.3
scikit-learn==1.5.2
numpy==1.26.4

# Notebook & utilities
jupyterlab==4.5.7          # CVE-2025-59842, CVE-2026-42266, CVE-2026-42557 fixed in 4.5.7
python-dotenv==1.2.2       # bumped from 1.0.1 to fix CVE-2026-28684
requests==2.34.2

# Visualization (used by baseline notebook EDA cells)
matplotlib==3.10.9
seaborn==0.13.2

# Legacy v2 baseline serving (src/serving/) — FastAPI, kept for "before" comparison
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4

# MCP runtime
uv==0.11.6                 # CVE-2025-54368, CVE-2025-13327 etc. fixed in 0.11.6+

# Test
pytest==9.0.3
